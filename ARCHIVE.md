# Historical release archive

The immutable Git tag `v0.6.7` preserves the former 0.6.x source and
documentation snapshot. It is retained as a historical record only.

The current branch intentionally contains only the 0.7.0 public boundary and
its matching migration, distribution, and conformance materials.
